const Sequelize = require('sequelize')
const sequelize = require('../config')

let = require('./.js')(sequelize, Sequelize)
let = require('./.js')(sequelize, Sequelize)
let = require('./.js')(sequelize, Sequelize)


.hasMany()
  .belongsTo()
  .hasMany()
  .belongsTo()


module.exports = {

}